{
  "name": "init_agentic_repo",
  "description": "Transforms a directory into a 100% compliant Agentic Repository with localized personas and multi-agent nexus stacks.",
  "parameters": {
    "type": "object",
    "properties": {
      "target_path": {
        "type": "string",
        "description": "Absolute path to the project directory to bootstrap."
      },
      "project_name": {
        "type": "string",
        "description": "Name of the project for manifest personalization."
      }
    },
    "required": ["target_path", "project_name"]
  }
}
