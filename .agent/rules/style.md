# Style & Architecture Rules
- **Formatting**: All files must follow the project-standard prettier/lint rules.
- **TDD**: No implementation without a characterization test (Rule 152).
- **Paths**: Always use absolute paths or project-root relative paths.
