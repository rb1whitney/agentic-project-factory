# Security & Privacy Rules
- **Credentials**: Never store API keys in plaintext. Use ~/.mcp-servers/credentials.
- **Boundaries**: Do not access the user's home directory outside the workspace without consent.
- **Data Exclusion**: Respect the exclusions in privacy.toml.
