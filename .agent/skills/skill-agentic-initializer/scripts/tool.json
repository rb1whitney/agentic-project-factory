{
  "name": "init_agentic_repo",
  "description": "Transforms a directory into a Vercel-style Agentic Repository with a compressed AGENTS.md manifest.",
  "parameters": {
    "type": "object",
    "properties": {
      "target_path": {
        "type": "string",
        "description": "Absolute path to the project directory to bootstrap."
      },
      "project_name": {
        "type": "string",
        "description": "Name of the project for manifest personalization."
      },
      "docs_dir": {
        "type": "string",
        "description": "Directory containing documentation to index (default: 'docs')."
      }
    },
    "required": ["target_path", "project_name"]
  }
}
