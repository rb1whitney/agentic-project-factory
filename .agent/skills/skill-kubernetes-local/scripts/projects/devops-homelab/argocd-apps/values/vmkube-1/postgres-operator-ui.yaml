ingress:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: postgres.vmkube-1.homelab.internal
  hosts:
    - host: postgres.vmkube-1.homelab.internal
      paths:
        - "/"
  tls:
    - secretName: postgres-ui-tls
      hosts:
        - postgres.vmkube-1.homelab.internal
